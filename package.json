{
  "name": "eomcalc",
  "version": "1.0.0",
  "description": "Echoes of Magic Skill Calculator",
  "scripts": {
    "build": "npx eslint src/js/** && node build",
    "serve": "node build --serve",
    "deploy": "npm run build && git push origin --delete gh-pages && git add dist -f && git commit -m 'auto deploy' && git subtree push --prefix dist origin gh-pages && git reset HEAD~"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/erhickey/eomcalc.git"
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.12.12",
    "eslint": "^7.29.0"
  }
}
